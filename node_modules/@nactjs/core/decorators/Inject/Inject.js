"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Inject = void 0;
const index_1 = require("../../nact-constants/index");
function createPropertyDecorator(func) {
    return function (target, key, index) {
        func(target, key, index);
    };
}
function Inject(token) {
    return createPropertyDecorator((target, key, index) => {
        const propToken = token || Reflect.getMetadata("design:type", target, key);
        if (propToken) {
            const dependencies = Reflect.getMetadata(index_1.PROPERTY_DEPENDENCIES, target) ?? [];
            Reflect.defineMetadata(index_1.PROPERTY_DEPENDENCIES, [...dependencies, { index: index, name: propToken, type: "inject" }], target);
        }
    });
}
exports.Inject = Inject;
