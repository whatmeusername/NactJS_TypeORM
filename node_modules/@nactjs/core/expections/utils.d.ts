import type { NactConfigItem } from "../routing";
import type { HttpExpectionHandler } from "./base-http-expection-handler.handler";
import type { HttpExpection } from "./base-http-expection.expection";
declare function getNamesForExpectionHandler(instances: ({
    new (status: number, message: string): HttpExpection;
} | HttpExpection | string)[]): string[];
declare function mapHandlers(handlers: ({
    new (): HttpExpectionHandler;
} | HttpExpectionHandler)[], storeAt: NactConfigItem[]): NactConfigItem[];
export { getNamesForExpectionHandler, mapHandlers };
//# sourceMappingURL=utils.d.ts.map